import { requestJsonSchemas, requestSchemas, } from '../generated/request-schemas.js';
import { targetMeta, targets } from '../generated/targets.js';
export class BundledSchema {
    static shared = new BundledSchema();
    getRequestSchema(target) {
        return requestSchemas[target];
    }
    listTargets() {
        return [...targets];
    }
    getTargetMeta(target) {
        return targetMeta[target];
    }
    getTargetParameterSchema(target) {
        return requestJsonSchemas[target];
    }
    getSharedParameters() {
        return {};
    }
}
