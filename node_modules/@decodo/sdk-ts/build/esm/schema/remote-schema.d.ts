import type { JSONSchema4 } from 'json-schema';
import * as z from 'zod';
import type { TargetMeta } from '../generated/targets.js';
import type { DecodoSchema, RemoteSchemaLoadOptions } from './types.js';
export declare class RemoteSchema implements DecodoSchema {
    readonly version: string;
    private readonly schemas;
    private readonly meta;
    private readonly parameterSchemas;
    private readonly sharedParameters;
    private readonly targetKeys;
    private constructor();
    static load(config?: RemoteSchemaLoadOptions): Promise<RemoteSchema>;
    getRequestSchema(target: string): z.ZodType<unknown, unknown, z.core.$ZodTypeInternals<unknown, unknown>> | undefined;
    listTargets(): string[];
    getTargetMeta(target: string): TargetMeta | undefined;
    getTargetParameterSchema(target: string): JSONSchema4 | undefined;
    getSharedParameters(): Record<string, unknown>;
}
