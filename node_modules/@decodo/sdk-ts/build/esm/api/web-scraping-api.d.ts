import { HttpClient } from '../http.js';
import type { DecodoSchema } from '../schema/types.js';
import type { ScrapeRequest, BatchRequest } from '../generated/targets.js';
import type { SyncResponse, AsyncTaskResponse, BatchResponse, TaskMetadata, TaskResultsResponse } from '../types/responses.js';
export declare class WebScrapingApi {
    private readonly http;
    private readonly schemas;
    constructor(http: HttpClient, schemas?: DecodoSchema);
    private validate;
    scrape(params: ScrapeRequest): Promise<SyncResponse>;
    scrapeAsync(params: ScrapeRequest): Promise<AsyncTaskResponse>;
    scrapeBatch(params: BatchRequest): Promise<BatchResponse>;
    getStatus(taskId: string): Promise<TaskMetadata>;
    getResults(taskId: string): Promise<TaskResultsResponse | null>;
}
